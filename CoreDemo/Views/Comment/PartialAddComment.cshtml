@*@model EntityLayer.Concrete.Comment*@

<div class="comment-top">
    <h4>Bir yorum yapın</h4>
    <div class="comment-bottom">

        @*@using (Html.BeginForm("PartialAddComment", "Comment", FormMethod.Post))
        {
            @Html.TextBoxFor(x => x.CommenrUserName, new { @class = "form-control", @placeholder = "İsim", @required = "" })
            @Html.TextBoxFor(x => x.CommentTitle, new { @class = "form-control", @placeholder = "Konu", @required = "" })
            @Html.TextAreaFor(x => x.CommentContent, new { @class = "form-control", @placeholder = "Yorumuzun...", @required = "" })
            <button class="btn btn-primary submit">Yorumu Gönder</button>
        }*@


        <form action="/Comment/PartialAddComment/" method="post">
            <input class="form-control" type="text" name="CommenrUserName" placeholder="İsim" required="">
            
            <input class="form-control" type="text" name="CommentTitle" placeholder="Konu" required="">

            <textarea class="form-control" name="CommentContent" placeholder="Yorumuzun..." required=""></textarea>
            <button type="submit" class="btn btn-primary submit">Yorumu Gönder</button>
        </form>
    </div>
</div>
